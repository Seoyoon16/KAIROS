# 삼항연산자 포맷팅
# print(" %s 맛있게 드세요!"%(res) if enter < 4 else "잘못된 번호 입력")

# 람다로 제곱 계산
# square = lambda x, y : x ** y
# result = square(5, 2)

# 람다로 문자열 길이 순 정렬
# strings = ['apple', 'banana', 'kiwi', 'orange']
# sorted_strings = sorted(strings, key=lambda x: len(x))

# 람다로 조건부 반환
# check_value = lambda x: 'Even' if x % 2 == 0 else 'Odd'
# print(check_value(7))  # 결과: Odd

# 람다 +  map 함수 + 리스트 덧셈
# numbers = [1, 2, 3, 4, 5]
# updated_numbers = list(map(lambda x: x + 2, numbers))


